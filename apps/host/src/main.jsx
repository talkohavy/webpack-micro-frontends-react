// NOTE: Webpack expects a full path here since you are using "type: module" inside your package.json
/* eslint-disable import/extensions */
import('./bootstrap.jsx');

// import './bootstrap.jsx'; // <--- this doesn't work!!
